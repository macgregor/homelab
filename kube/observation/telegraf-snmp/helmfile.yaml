repositories:
- name: influxdata
  url: https://helm.influxdata.com/
helmDefaults:
  createNamespace: true
releases:
- name: telegraf-snmp
  chart: influxdata/telegraf
  namespace: obs
  values:
  - ./helm-values.yml.gotmpl
