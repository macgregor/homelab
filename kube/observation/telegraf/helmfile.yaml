repositories:
- name: influxdata
  url: https://helm.influxdata.com/
helmDefaults:
  createNamespace: true
releases:
- name: telegraf
  chart: influxdata/telegraf-ds
  namespace: obs
  values:
  - ./helm-values.yml
