# {{ ansible_managed }}
#
# See journald.conf(5) for details.

[Journal]
# Store journal in RAM only (/run/log/journal/) to avoid SD card writes.
# Container logs are unaffected (managed by kubelet at /var/log/pods/).
# To temporarily persist logs for troubleshooting: set Storage=auto and restart journald.
Storage=volatile
Compress=yes
RuntimeMaxUse=20M
RateLimitIntervalSec=30s
RateLimitBurst=1000
ForwardToSyslog=no
Audit=
